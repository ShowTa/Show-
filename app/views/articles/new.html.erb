<%#= react_component('ArticleNew') %>

<!-- 以下のformでパラメータ飛ばすと記事はarticles,タグはtagsテーブルに保存される -->
<%= form_for @article do |f| %>
  <%= f.text_field :title %>
  <%= f.text_field :tag_list %>
  <%= f.submit %>
<% end %>
